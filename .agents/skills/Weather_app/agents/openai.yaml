interface:
  display_name: "Weather App"
  short_description: "Repo-specific patterns and workflows for Weather_app"
  default_prompt: "Use the Weather_app repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true